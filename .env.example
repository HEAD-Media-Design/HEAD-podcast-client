# Copy to `.env`. No trailing slash.
# On Vercel, set this as a Project Environment Variable (Production/Preview/Development)
# instead — env vars set on the platform win over this file (see scripts/load-root-env.ts).
VITE_SITE_URL=https://your-project.vercel.app
